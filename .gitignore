node_modules/
dist/
dist-single/
.DS_Store
Thumbs.db
*.log
.env
.env.local

# Claude Code local settings
.claude/settings.local.json

# Session audit temp markers (not the audits themselves)
session_audits/.*.start

# Live demo sandbox - scaffolded fresh each talk, never committed.
# Keep the folder itself (via .gitkeep) so the structure is always present.
live_demo/sandbox/*
!live_demo/sandbox/.gitkeep
